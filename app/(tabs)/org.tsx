import { Text, View } from "react-native";

export default function Org() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text> ORG VIEW </Text>
    </View>
  );
}
